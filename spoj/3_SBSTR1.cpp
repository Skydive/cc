#include <iostream>
#include <cstdlib>
#include <cmath>
using namespace std;

inline int roll(int x, int b, int len) {
    return (x >> 1) + (b << len);
}

int main(int argc, char** argv) {
    

    return 0;
}